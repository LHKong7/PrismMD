// AnnotationMark is used to render highlighted text in the markdown body.
// It wraps annotated text with a colored background mark.
// This component is applied via DOM manipulation after rendering (see useAnnotations).
export {}
